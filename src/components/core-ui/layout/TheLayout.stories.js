import { TheLayout } from "./TheLayout";

export default {
  title: "Design System/Core Ui/Layout",
  component: TheLayout,
};

const Template = (args) => <TheLayout {...args} />;
export const Basic = Template.bind({});
