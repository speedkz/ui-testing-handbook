import { TheInput } from "./TheInput";

export default {
  title: "Design System/Core Ui/Input",
  component: TheInput,
};

const Template = (args) => <TheInput {...args} />;

export const Default = Template.bind({});
Default.args = {};
